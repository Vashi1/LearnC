#include <stdio.h>

int main(){
    printf("Enter your age: ");
    int a;
    scanf("%d", &a);
    if (a < 18){
        printf("You are not allowed to drive");
        }
    else{
        printf("You are allowed to drive");
    }
    }
